# Gemini 3.6 Flash

The live LLM adapter targets `gemini-3.6-flash` and uses Google's Interactions API.

The application-facing `LLMProvider` protocol remains unchanged. Provider-specific API details stay isolated in the infrastructure adapter.

Gemini 3.6 Flash is the default configured model for the project.
