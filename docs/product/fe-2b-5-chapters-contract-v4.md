# FE-2B.5 Chapters

The backend is authoritative for chapter state and persisted versions. The UI only owns transient selection and editor state.

The workspace supports chapter selection, persisted version inspection, generation, review, and author approval/rejection.
